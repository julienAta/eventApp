import { getEvents, addEvent, updateEvent, deleteEvent } from "./eventModel";
import { users, addUser, updateUser, deleteUser } from "./userModel";

export {
  getEvents,
  addEvent,
  updateEvent,
  deleteEvent,
  users,
  addUser,
  updateUser,
  deleteUser,
};
